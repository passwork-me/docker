server {
    listen 8080;
    location /nginx_status {
        stub_status on;
        access_log off;
        allow 127.0.0.1;
        allow 192.168.0.0/16;
        allow 172.0.0.0/8;
        deny all;
    }

    location /munin {
        alias /server/sites/munin;
        autoindex on;
    }
}
